__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
reports/

# Agent configuration
CLAUDE.md

# Foundry / on-chain artifacts
onchain/out/
onchain/cache/
onchain/broadcast/
onchain/lib/
TODO.md

# Local run config (may hold endpoint URLs / rpc-url); the .example is committed.
.x402-conformance.toml

# Default run-record directory (audit trail of runs) — machine-local, not tracked.
x402-runs/
